# Python Baseline Dependencies
# 
# For GPU acceleration (cuML/RAPIDS):
#   conda install -c rapidsai -c conda-forge -c nvidia cuml cudf
#
# For attestation bindings:
#   pip install maturin
#   ./build.sh

scikit-learn>=1.3.0
numpy>=1.24.0
maturin>=1.4.0
