# Stub module - will be replaced by compiled Rust extension
# This allows the code to run without the compiled module (with reduced functionality)

raise ImportError("tee_attestation Rust module not built. Run: cd tee_attestation && maturin develop --release")
