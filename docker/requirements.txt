# ═══════════════════════════════════════════════════════════════════════════
# Python Dependencies for Confidential AI Healthcare Demo
# ═══════════════════════════════════════════════════════════════════════════
#
# IMPORTANT: Use conda for GPU/RAPIDS dependencies, NOT pip!
#
# Setup command:
#   conda create -n rapids-24.10 -c rapidsai -c conda-forge -c nvidia \
#       rapids=24.10 python=3.11 cuda-version=12.4 scikit-learn
#
# Then install these additional pip packages:
# ═══════════════════════════════════════════════════════════════════════════

# Core ML (already in conda, but listed for reference)
scikit-learn>=1.3.0
joblib>=1.4.0

# HTTP requests (for NRAS attestation)
requests>=2.31.0

# NVIDIA Attestation SDK
nv-attestation-sdk>=2.6.0

# PyO3 build tool (for tee_attestation module)
maturin>=1.4.0
